# Cosine Companion - Python Dependencies

# This is the human-facing declaration of supported dependency ranges. The
# macOS app build and its test job install requirements-macos-arm64-py311.lock
# instead so release inputs are fully reproducible. Update and validate that
# lock whenever a direct requirement changes here.

# Core data processing
numpy>=1.20.0
pandas>=1.3.0
pyarrow>=12

# Audio processing
soundfile>=0.10.0
essentia-tensorflow>=2.1b6.dev0

# XML parsing
lxml>=4.6.0

# CLI interface
typer>=0.4.0

# Web UI (`ui`; `ui-web` remains as a compatibility alias)
# The ONLY new runtime dependency of the web backbone. Everything else the web
# layer needs is in the standard library - the HTTP server is
# http.server.ThreadingHTTPServer and the frontend has no build step.
pywebview>=5.0

# Native macOS window integration
pyobjc-framework-Cocoa>=9.0; sys_platform == 'darwin'  # For macOS dock icon

# Packaging (optional - for building standalone app)
pyinstaller>=5.0.0
